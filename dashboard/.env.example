# Copy to .env and fill in values
# docker compose --env-file .env up -d --build

DB_PASSWORD=your_db_password_here
REDIS_PASSWORD=change_me_see_1password
DASHBOARD_PORT=3000
DASHBOARD_API_KEY=
LOG_LEVEL=INFO
